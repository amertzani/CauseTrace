# Full dependency list (reference only).
# One-shot "pip install -r requirements.txt" can hit ResolutionTooDeep.
# Use two-stage install:
#
#   pip install -r requirements-core.txt
#   BLIS_ARCH=generic pip install -r requirements-ml.txt
#
# Or run: ./install_requirements.sh

# --- requirements-core.txt ---
# rdflib>=6.0.0, huggingface_hub, PyPDF2, python-docx, pandas, openpyxl,
# chardet, networkx, matplotlib, plotly, numpy>=1.24,<2.0, fastapi,
# uvicorn[standard], pydantic, python-multipart, clean-text>=0.6.0

# --- requirements-ml.txt (install with BLIS_ARCH=generic) ---
# gradio==4.44.0, spacy==3.7.6, transformers, torch, accelerate, bitsandbytes,
# causal-learn==0.1.4.4, dowhy==0.14
